---

<%
  filter = Proc.new {|name,val| [name.downcase, val]}
  auth_vars = user_context_to_vars('admin', :file, filter)
  auth_vars ||= user_context_to_vars('admin', :memory, filter)
%>
<%- auth_vars.each do |name, val| %>
<%= name %>: <%= val %>
<% end %>
storage_vip:  <%="#{node[:bcpc][:floating][:vip]}"%>
storage_port: <%="#{node[:bcpc][:ports][:radosgw]}" %>
